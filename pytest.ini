[pytest]
# importlib mode lets the two test packages (pipeline/tests and review_site/tests,
# both named "tests") be collected together without a module-name clash.
addopts = --import-mode=importlib
testpaths = pipeline/tests review_site/tests
